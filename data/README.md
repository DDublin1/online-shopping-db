# Data

This repository contains the full database schema and analytical queries in `sql/schema.sql`.
Sample data for the Customers, Products, Orders, Order Items, and Payments tables is included
directly in the SQL file — no external data files are required.

To set up the database, run `sql/schema.sql` against a SQL Server instance.
